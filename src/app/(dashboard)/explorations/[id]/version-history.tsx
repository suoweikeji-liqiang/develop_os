'use client'

// Legacy compatibility wrapper: Requirement version history now lives under
// `requirements/[id]`. Keep this file only for `/explorations/[id]` aliases.
export { VersionHistory } from '../../requirements/[id]/version-history'
