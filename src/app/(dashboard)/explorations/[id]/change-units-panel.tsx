'use client'

// Legacy compatibility wrapper: Requirement change queue now lives under
// `requirements/[id]`. Keep this file only for `/explorations/[id]` aliases.
export { ChangeUnitsPanel } from '../../requirements/[id]/change-units-panel'
