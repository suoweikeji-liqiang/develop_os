'use client'

// Legacy compatibility wrapper: Requirement verification assets now belong to
// the Requirement worksurface. Keep this file only for `/explorations/[id]` aliases.
export { TestCasePanel } from '../../requirements/[id]/test-case-panel'
