'use client'

// Legacy compatibility wrapper: Requirement signoff now belongs to the
// Requirement worksurface. Keep this file only for `/explorations/[id]` aliases.
export { SignoffPanel } from '../../requirements/[id]/signoff-panel'
