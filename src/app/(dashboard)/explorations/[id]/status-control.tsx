'use client'

// Legacy compatibility wrapper: Requirement status control now belongs to the
// Requirement worksurface. Keep this file only for `/explorations/[id]` aliases.
export { StatusControl } from '../../requirements/[id]/status-control'
