'use client'

// Legacy compatibility wrapper: Requirement comments now live under
// `requirements/[id]`. Keep this file only so `/explorations/[id]` aliases and
// older imports do not regain product ownership.
export { CommentsPanel } from '../../requirements/[id]/comments-panel'
