'use client'

// Legacy compatibility wrapper: Requirement consensus status now belongs to the
// Requirement worksurface. Keep this file only for `/explorations/[id]` aliases.
export { ConsensusStatus } from '../../requirements/[id]/consensus-status'
